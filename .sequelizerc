const path = require('path'); //dependência do node (nativo)  mostra o caminho do arquivo que vamos configurar

module.exports ={
    config: path.resolve(__dirname, 'src', 'config', 'database.js'),
}